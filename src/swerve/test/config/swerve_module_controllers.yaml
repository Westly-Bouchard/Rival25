controller_manager:
  ros__parameters:
    update_rate: 100

forward_position_controller:
  ros__parameters:
    type: forward_command_controller/ForwardCommandController
    joints:
      - azimuth
    interface_name: az_target_position

forward_velocity_controller:
  ros__parameters:
    type: forward_command_controller/ForwardCommandController
    joints:
      - drive
    interface_name: drive_target_velocity
